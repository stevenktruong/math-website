import { publicRuntimeConfig } from "helpers";

export default linkedinMeta = (
    <>
        <meta property="og:image" content={`${publicRuntimeConfig.staticFolder}/images/linkedin-preview.webp`} />
    </>
);
